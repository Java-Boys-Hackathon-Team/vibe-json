# Скопируй в .env рядом с docker-compose.yml и подставь значения.
# Ключ можно оставить заглушкой - приложение поднимется, не работать будут
# только обращения к модели.
OPENAI_API_KEY=sk-stub-not-a-real-key
